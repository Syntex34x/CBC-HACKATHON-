name: planner
role: requirement_engine

input: user_prompt

output:
  - problem_breakdown
  - feature_map
  - tech_stack
  - constraints

logic:
  - extract intent
  - classify app type
  - define modules
  - map dependencies
